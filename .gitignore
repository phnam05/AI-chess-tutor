# Secrets — never commit your API key
.env
.streamlit/secrets.toml

# The Stockfish engine binary (users download their own)
stockfish.exe
stockfish

# Python
venv/
.venv/
__pycache__/
*.pyc

# Runtime logs
*.log
faithfulness_log.jsonl

# Editor / OS noise
.vscode/
.idea/
.DS_Store

#secret file
leandro.txt

# Lab chat export (Discord HTML): holds private personal data, never publish
NESPeD-Lab*.html

# Private: notes from the lab chat (repo is public) and the frozen long diary
# that quotes them. Both stay on the laptop only.
lab_notes.md
DEV_DIARY_full.md
